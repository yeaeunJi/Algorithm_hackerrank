def birthdayCakeCandles(candles):
    answer  = 0
    
    max_val = max(candles)
    
    for candle in candles :
        if candle == max_val :
            answer += 1
        else :
            pass
    
    return answer
